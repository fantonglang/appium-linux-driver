const commands = {};

commands.getScreenshot = async function () {
  return null; // base64 png
};

commands.getElementScreenshot = async function (element) {
  return null; // base64 png
};

export default commands;
